#include "main.h"
/**
 * _abs - computes a specific interger
 * @n: a given value
 * Return: value if n is greater to zero, other value
 */
int _abs(int n)
{

	if (n >= 0)
		return (n);
	else
		return (-n);
